services:
  db:
    image: mariadb:11
    container_name: northstar-db
    restart: unless-stopped
    environment:
      MARIADB_ROOT_PASSWORD: admin
      MARIADB_DATABASE: northstar_marketplace
    ports:
      - "3306:3306"
    volumes:
      - northstar-data:/var/lib/mysql
    healthcheck:
      test: ["CMD", "healthcheck.sh", "--connect", "--innodb_initialized"]
      interval: 5s
      timeout: 5s
      retries: 10

volumes:
  northstar-data:
